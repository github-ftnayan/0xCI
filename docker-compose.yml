services:
  probot:
    build:
      context: packages/probot-app
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./packages/probot-app:/app
      - /app/node_modules
    env_file:
      - packages/probot-app/.env
